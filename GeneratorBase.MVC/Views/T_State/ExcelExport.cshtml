@model PagedList.IPagedList<GeneratorBase.MVC.Models.T_State>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Index";
    Layout = null;
    Response.ContentType = "application/vnd.ms-excel";
    Response.AddHeader("Content-Disposition", "attachment; filename=State.xls");
}
                    <table>
                            <tr>
@if(User.CanView("T_State","T_Name"))
{
		<th>Name</th>
}
@if(User.CanView("T_State","T_Description"))
{
		<th>Description</th>
}
@if(User.CanView("T_State","T_StateCountryID"))
{
		<th  @(Convert.ToString(ViewData["HostingEntity"]) == "T_Country"?"hidden":"")>State Country</th>
}
    </tr>
@foreach (var item in Model) {
    <tr>
	@if(User.CanView("T_State","T_Name"))
{
		<td>
            @Html.DisplayFor(modelItem => item.T_Name)
        </td>
}
@if(User.CanView("T_State","T_Description"))
{
		<td>
            @Html.DisplayFor(modelItem => item.T_Description)
        </td>
}
@if(User.CanView("T_State","T_StateCountryID"))
{
		<td @(Convert.ToString(ViewData["HostingEntity"]) == "T_Country"?"hidden":"")>
            @Html.DisplayFor(modelItem => item.t_statecountry.DisplayValue)
        </td>
}
                                </tr>
                            }
</table>
               
