@model PagedList.IPagedList<GeneratorBase.MVC.Models.T_ClaimType>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Index";
    Layout = null;
    Response.ContentType = "application/vnd.ms-excel";
    Response.AddHeader("Content-Disposition", "attachment; filename=Claim Type.xls");
}
                    <table>
                            <tr>
@if(User.CanView("T_ClaimType","T_ClaimTypeFacilityAssociationID"))
{
		<th  @(Convert.ToString(ViewData["HostingEntity"]) == "T_Facility"?"hidden":"")>Facility</th>
}
@if(User.CanView("T_ClaimType","T_Name"))
{
		<th>Name</th>
}
    </tr>
@foreach (var item in Model) {
    <tr>
	@if(User.CanView("T_ClaimType","T_ClaimTypeFacilityAssociationID"))
{
		<td @(Convert.ToString(ViewData["HostingEntity"]) == "T_Facility"?"hidden":"")>
            @Html.DisplayFor(modelItem => item.t_claimtypefacilityassociation.DisplayValue)
        </td>
}
@if(User.CanView("T_ClaimType","T_Name"))
{
		<td>
            @Html.DisplayFor(modelItem => item.T_Name)
        </td>
}
                                </tr>
                            }
</table>
               
