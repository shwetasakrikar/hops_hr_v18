@model PagedList.IPagedList<GeneratorBase.MVC.Models.T_ShiftDuration>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Index";
    Layout = null;
    Response.ContentType = "application/vnd.ms-excel";
    Response.AddHeader("Content-Disposition", "attachment; filename=Shift Duration.xls");
}
                    <table>
                            <tr>
@if(User.CanView("T_ShiftDuration","T_Name"))
{
		<th>Name</th>
}
    </tr>
@foreach (var item in Model) {
    <tr>
	@if(User.CanView("T_ShiftDuration","T_Name"))
{
		<td>
            @Html.DisplayFor(modelItem => item.T_Name)
        </td>
}
                                </tr>
                            }
</table>
               
