@model PagedList.IPagedList<GeneratorBase.MVC.Models.T_ConversationalEmployeeForeignLanguage>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Index";
    Layout = null;
    Response.ContentType = "application/vnd.ms-excel";
    Response.AddHeader("Content-Disposition", "attachment; filename=Conversational Employee Foreign Language.xls");
}
                    <table>
                            <tr>
@if(User.CanView("T_ConversationalEmployeeForeignLanguage","T_EmployeeID"))
{
		<th  @(Convert.ToString(ViewData["HostingEntity"]) == "T_Employee"?"hidden":"")>Employees Conversational In</th>
}
@if(User.CanView("T_ConversationalEmployeeForeignLanguage","T_LangaugeID"))
{
		<th  @(Convert.ToString(ViewData["HostingEntity"]) == "T_Langauge"?"hidden":"")>Conversational Foreign Language</th>
}
    </tr>
@foreach (var item in Model) {
    <tr>
	@if(User.CanView("T_ConversationalEmployeeForeignLanguage","T_EmployeeID"))
{
		<td @(Convert.ToString(ViewData["HostingEntity"]) == "T_Employee"?"hidden":"")>
            @Html.DisplayFor(modelItem => item.t_employee.DisplayValue)
        </td>
}
@if(User.CanView("T_ConversationalEmployeeForeignLanguage","T_LangaugeID"))
{
		<td @(Convert.ToString(ViewData["HostingEntity"]) == "T_Langauge"?"hidden":"")>
            @Html.DisplayFor(modelItem => item.t_langauge.DisplayValue)
        </td>
}
                                </tr>
                            }
</table>
               
