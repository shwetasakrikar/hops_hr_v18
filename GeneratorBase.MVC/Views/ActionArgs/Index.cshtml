@model IEnumerable<GeneratorBase.MVC.Models.ActionArgs>
@{
    ViewBag.Title = "Action Args List";
	 if (ViewBag.CurrentSort != null || ViewBag.Pages != null || ViewData["HostingEntity"] != null || ViewBag.CurrentFilter != null || ViewBag.CurrentItemsPerPage != null)
    {
         if (ViewBag.FSFilter == null)
        {
            Layout = null;
        }
    }
}
<div id="ActionArgs">
 @if(ViewData["HostingEntity"] == null)
 { 
<div class="row">
    <div class="col-lg-12">
        <h1 class="page-title">
            <i class="glyphicon glyphicon-thumbs-up text-primary" style="margin-bottom:-20px;"></i> Action Args  <i class="glyphicon glyphicon-chevron-right small"></i>  <span>List</span>
</h1>
    </div>
</div>
}
    <!-- /.col-lg-12 -->
@Html.Partial("~/Views/ActionArgs/IndexPartial.cshtml", Model)
</div>
