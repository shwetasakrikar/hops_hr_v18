@model PagedList.IPagedList<GeneratorBase.MVC.Models.T_MachineTool>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Index";
    Layout = null;
    Response.ContentType = "application/vnd.ms-excel";
    Response.AddHeader("Content-Disposition", "attachment; filename=Machine Tool.xls");
}
                    <table>
                            <tr>
@if(User.CanView("T_MachineTool","T_Name"))
{
		<th>Name</th>
}
@if(User.CanView("T_MachineTool","T_Description"))
{
		<th>Description</th>
}
    </tr>
@foreach (var item in Model) {
    <tr>
	@if(User.CanView("T_MachineTool","T_Name"))
{
		<td>
            @Html.DisplayFor(modelItem => item.T_Name)
        </td>
}
@if(User.CanView("T_MachineTool","T_Description"))
{
		<td>
            @Html.DisplayFor(modelItem => item.T_Description)
        </td>
}
                                </tr>
                            }
</table>
               
