@model PagedList.IPagedList<GeneratorBase.MVC.Models.T_TestType>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Index";
    Layout = null;
    Response.ContentType = "application/vnd.ms-excel";
    Response.AddHeader("Content-Disposition", "attachment; filename=Test Type.xls");
}
                    <table>
                            <tr>
@if(User.CanView("T_TestType","T_Name"))
{
		<th>Name</th>
}
    </tr>
@foreach (var item in Model) {
    <tr>
	@if(User.CanView("T_TestType","T_Name"))
{
		<td>
            @Html.DisplayFor(modelItem => item.T_Name)
        </td>
}
                                </tr>
                            }
</table>
               
