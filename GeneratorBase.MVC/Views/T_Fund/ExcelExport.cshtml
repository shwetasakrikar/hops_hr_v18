@model PagedList.IPagedList<GeneratorBase.MVC.Models.T_Fund>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Index";
    Layout = null;
    Response.ContentType = "application/vnd.ms-excel";
    Response.AddHeader("Content-Disposition", "attachment; filename=Fund.xls");
}
                    <table>
                            <tr>
@if(User.CanView("T_Fund","T_Code"))
{
		<th>Code</th>
}
@if(User.CanView("T_Fund","T_Description"))
{
		<th>Description</th>
}
    </tr>
@foreach (var item in Model) {
    <tr>
	@if(User.CanView("T_Fund","T_Code"))
{
		<td>
            @Html.DisplayFor(modelItem => item.T_Code)
        </td>
}
@if(User.CanView("T_Fund","T_Description"))
{
		<td>
            @Html.DisplayFor(modelItem => item.T_Description)
        </td>
}
                                </tr>
                            }
</table>
               
