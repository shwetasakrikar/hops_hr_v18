@model PagedList.IPagedList<GeneratorBase.MVC.Models.T_LeaveProfile>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Index";
    Layout = null;
    Response.ContentType = "application/vnd.ms-excel";
    Response.AddHeader("Content-Disposition", "attachment; filename=Leave.xls");
}
                    <table>
                            <tr>
@if(User.CanView("T_LeaveProfile","T_EmployeeLeaveProfileID"))
{
		<th  @(Convert.ToString(ViewData["HostingEntity"]) == "T_Employee"?"hidden":"")>Employee</th>
}
@if(User.CanView("T_LeaveProfile","T_From"))
{
		<th>From Date</th>
}
@if(User.CanView("T_LeaveProfile","T_To"))
{
		<th>To Date</th>
}
@if(User.CanView("T_LeaveProfile","T_DaysOff"))
{
		<th>Days Off</th>
}
@if(User.CanView("T_LeaveProfile","T_Intermittent"))
{
		<th>Intermittent</th>
}
@if(User.CanView("T_LeaveProfile","T_FullDays"))
{
		<th>Full Days</th>
}
@if(User.CanView("T_LeaveProfile","T_InjuryRelatedLeaveID"))
{
		<th  @(Convert.ToString(ViewData["HostingEntity"]) == "T_EmployeeInjury"?"hidden":"")>Related Injury If Any</th>
}
@if(User.CanView("T_LeaveProfile","T_Notes"))
{
		<th>Notes</th>
}
    </tr>
@foreach (var item in Model) {
    <tr>
	@if(User.CanView("T_LeaveProfile","T_EmployeeLeaveProfileID"))
{
		<td @(Convert.ToString(ViewData["HostingEntity"]) == "T_Employee"?"hidden":"")>
            @Html.DisplayFor(modelItem => item.t_employeeleaveprofile.DisplayValue)
        </td>
}
@if(User.CanView("T_LeaveProfile","T_From"))
{
		<td>
            @Html.DisplayFor(modelItem => item.T_From)
        </td>
}
@if(User.CanView("T_LeaveProfile","T_To"))
{
		<td>
            @Html.DisplayFor(modelItem => item.T_To)
        </td>
}
@if(User.CanView("T_LeaveProfile","T_DaysOff"))
{
		<td>
            @Html.DisplayFor(modelItem => item.T_DaysOff)
        </td>
}
@if(User.CanView("T_LeaveProfile","T_Intermittent"))
{
		<td>
            @Html.DisplayFor(modelItem => item.T_Intermittent)
        </td>
}
@if(User.CanView("T_LeaveProfile","T_FullDays"))
{
		<td>
            @Html.DisplayFor(modelItem => item.T_FullDays)
        </td>
}
@if(User.CanView("T_LeaveProfile","T_InjuryRelatedLeaveID"))
{
		<td @(Convert.ToString(ViewData["HostingEntity"]) == "T_EmployeeInjury"?"hidden":"")>
            @Html.DisplayFor(modelItem => item.t_injuryrelatedleave.DisplayValue)
        </td>
}
@if(User.CanView("T_LeaveProfile","T_Notes"))
{
		<td>
            @Html.DisplayFor(modelItem => item.T_Notes)
        </td>
}
                                </tr>
                            }
</table>
               
