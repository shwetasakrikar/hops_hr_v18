@using Microsoft.AspNet.Identity
@{
    ViewBag.Title = "About";
}
<div class="row">
    <div class="col-lg-12">
        <h1 class="page-title">
            <i class="glyphicon glyphicon-home text-primary" style="margin-bottom:-20px;"></i> About Application
        </h1>
    </div>
    <!-- /.col-lg-12 -->
</div>
<div class="row">
    <div class="col-sm-12">
        @if (ViewBag.AboutCompany != null)
        {
            @Html.Raw(ViewBag.AboutCompany)
        }
    </div>
</div>