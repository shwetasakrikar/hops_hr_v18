@model PagedList.IPagedList<GeneratorBase.MVC.Models.FeedbackPriority>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Index";
    Layout = null;
    Response.ContentType = "application/vnd.ms-excel";
    Response.AddHeader("Content-Disposition", "attachment; filename=FeedbackPriority.xls");
}
                    <table>
                            <tr>
@if(User.CanView("FeedbackPriority","Name"))
{
		<th>Name</th>
}
@if(User.CanView("FeedbackPriority","Description"))
{
		<th>Description</th>
}
    </tr>
@foreach (var item in Model) {
    <tr>
	@if(User.CanView("FeedbackPriority","Name"))
{
		<td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
}
@if(User.CanView("FeedbackPriority","Description"))
{
		<td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
}
                                </tr>
                            }
</table>
               
