@model PagedList.IPagedList<GeneratorBase.MVC.Models.T_SalaryBand>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Index";
    Layout = null;
    Response.ContentType = "application/vnd.ms-excel";
    Response.AddHeader("Content-Disposition", "attachment; filename=Salary Band.xls");
}
                    <table>
                            <tr>
@if(User.CanView("T_SalaryBand","T_Name"))
{
		<th>Band</th>
}
    </tr>
@foreach (var item in Model) {
    <tr>
	@if(User.CanView("T_SalaryBand","T_Name"))
{
		<td>
            @Html.DisplayFor(modelItem => item.T_Name)
        </td>
}
                                </tr>
                            }
</table>
               
