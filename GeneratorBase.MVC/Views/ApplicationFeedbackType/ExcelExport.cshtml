@model PagedList.IPagedList<GeneratorBase.MVC.Models.ApplicationFeedbackType>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Index";
    Layout = null;
    Response.ContentType = "application/vnd.ms-excel";
    Response.AddHeader("Content-Disposition", "attachment; filename=ApplicationFeedbackType.xls");
}
                    <table>
                            <tr>
@if(User.CanView("ApplicationFeedbackType","Name"))
{
		<th>Name</th>
}
@if(User.CanView("ApplicationFeedbackType","Description"))
{
		<th>Description</th>
}
    </tr>
@foreach (var item in Model) {
    <tr>
	@if(User.CanView("ApplicationFeedbackType","Name"))
{
		<td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
}
@if(User.CanView("ApplicationFeedbackType","Description"))
{
		<td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
}
                                </tr>
                            }
</table>
               
