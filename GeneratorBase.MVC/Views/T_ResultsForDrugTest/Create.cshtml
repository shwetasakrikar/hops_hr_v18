@model GeneratorBase.MVC.Models.T_ResultsForDrugTest
@{
    ViewBag.Title = "Create Results For Drug Test";
	var EntityDisplayNameReflector = ModelReflector.Entities.FirstOrDefault(p => p.Name == "T_ResultsForDrugTest");
    var EntityDisplayName = EntityDisplayNameReflector != null ? EntityDisplayNameReflector.DisplayName : "Results For Drug Test";
}
<div class="row">
    <div class="col-lg-12">
        <h1 class="page-title" style="margin-bottom:10px">
		@{
                var hostingValue = "";
                if (ViewBag.DisplayVal != null)
                {
                    hostingValue = " - " + ViewBag.DisplayVal;
                }
            }
            <i class="glyphicon glyphicon-save text-primary"></i> @EntityDisplayName  <i class="glyphicon glyphicon-chevron-right small"></i> <span> Add Results For Drug Test @hostingValue</span>
			<a class="btn btn-xs pull-right" type="button" data-original-title="List View" data-placement="top" onclick=@Html.Raw("NavigateToUrl('" + Url.Action("Index", "T_ResultsForDrugTest") + "');") alt="Search" title="List">
				<i class="glyphicon glyphicon-list"></i> List
			</a>
			</div>
	

        </h1>
        <h2 class="text-primary">@Html.DisplayFor(model => model.DisplayValue)</h2>
    </div>
    <!-- /.col-lg-12 -->
</div>
@Html.Partial("~/Views/T_ResultsForDrugTest/CreatePartial.cshtml", Model)
