@model PagedList.IPagedList<GeneratorBase.MVC.Models.T_ShiftMealTime>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Index";
    Layout = null;
    Response.ContentType = "application/vnd.ms-excel";
    Response.AddHeader("Content-Disposition", "attachment; filename=Shift Meal Time.xls");
}
                    <table>
                            <tr>
@if(User.CanView("T_ShiftMealTime","T_MealTime"))
{
		<th>Meal Time</th>
}
    </tr>
@foreach (var item in Model) {
    <tr>
	@if(User.CanView("T_ShiftMealTime","T_MealTime"))
{
		<td>
            @Html.DisplayFor(modelItem => item.T_MealTime)
        </td>
}
                                </tr>
                            }
</table>
               
